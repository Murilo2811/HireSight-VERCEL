{
  "name": "hiresight-app",
  "version": "1.0.0",
  "description": "HireSight: AI-Powered Talent Analysis V2",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir public && cp index.html public/ && cp *.tsx public/ && cp *.ts public/ && cp -r components public/ && cp -r contexts public/ && cp -r locales public/ && cp -r services public/ && cp -r utils public/"
  },
  "dependencies": {
    "@google/genai": "^1.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  }
}